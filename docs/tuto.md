Mi locotron'!
Te explico: abre una terminal en la raiz del proyecto y ejecuta `php -S localhost:3000 router.php`. Luego abre `http://localhost:3000` en el navegador para probar la aplicacion con la API PHP actual.
